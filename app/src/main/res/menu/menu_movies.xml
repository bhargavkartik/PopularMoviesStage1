<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/action_popular"
        android:title="@string/movies_by_popularity"
        android:onClick="onSortByPopularityMenuItemClick"
        app:showAsAction="never" />

    <item
        android:id="@+id/action_top_rated"
        android:title="@string/movies_by_rating"
        android:onClick="onSortByRatingMenuItemClick"
        app:showAsAction="never" />

</menu>